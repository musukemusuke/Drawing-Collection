name: Run Discord Bot

on:
  push:
    branches: [ main ]
  schedule:
    # 3時間ごとに自動起動してバトンを繋ぐ（6時間制限の対策）
    - cron: '0 */3 * * *'
  workflow_dispatch: # 手動で起動できるボタンを追加

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
    # ★Node 24環境に完全ネイティブ対応した、正真正銘の最新メジャーバージョン「@v5」を指定
    - name: Checkout code safely
      uses: actions/checkout@v5

    # ★Node 24環境に完全ネイティブ対応した、公式Python部品の最新メジャーバージョン「@v6」を指定
    - name: Set up Python safely
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    # ボットに必要な部品のインストール
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    # 6時間制限にかかる前に、安全に自己終了させる命令を実行
    - name: Start Bot
      env:
        DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        PYTHONUNBUFFERED: 1 # Pythonの出力バッファリングを無効化してログをリアルタイム表示
      run: |
        python main.py
